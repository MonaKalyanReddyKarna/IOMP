import React from 'react'

// This is the main page of the app, write only calling for components here, main page will be in components folder

const Main = () => {
    return (
        <div className='w-full h-screen'>
        </div>
    )
}

export default Main